Can you carefully review, debug, and fix the entire Mood Logging system (including the DASS-21 assessment integration) in my Emotion Track website built with Python (backend) and HTML/JavaScript (frontend)? The current system is not saving mood logs correctly.
________________________________________
Main Bug to Fix
•	When I try to log my mood and complete the DASS-21 assessment, I get this error:
"Invalid emotion data. Please try again."
•	This prevents the mood log and DASS-21 results from being saved into the database.
________________________________________
Expected Behavior
1.	On the Log Emotions page:
o	The student selects their current emotion(s).
o	Immediately after selection, the DASS-21 assessment form should appear.
2.	Once the student completes the DASS-21 questionnaire:
o	The "Save my Mood Log" button becomes enabled.
o	When clicked, it should:
	Validate the emotion data and DASS-21 results.
	Save both the emotion log and DASS-21 results to the database (linked to the current student’s user_id).
	Show a success confirmation message (e.g., “Mood log saved successfully.”).
3.	If any required field is missing other than the Gratitude Note (which is optional), the system should show a clear validation error message (e.g., “Please select at least one emotion” or “Please complete all DASS-21 questions”), instead of the generic “Invalid emotion data”.
________________________________________
Debugging and Fix Instructions
1.	Backend (Python)
o	Check the route/controller that handles the Save Mood Log submission.
o	Ensure it:
	Properly receives both emotion data and DASS-21 answers from the frontend request.
	Validates that data correctly (fix any conditions that wrongly reject valid inputs).
	Inserts both into the correct tables in the database.
2.	Frontend (HTML/JS)
o	Make sure the Log Emotions form passes the selected emotions and the completed DASS-21 answers together in the request.
o	Debug the JavaScript to confirm it is sending valid JSON/form data to the backend.
o	Add clear form validation before submission.
3.	Database
o	Ensure the database schema supports saving:
	Student ID (user_id)
	Selected emotions
	DASS-21 scores/responses
	Timestamp
o	Verify that the backend query correctly inserts all these values without errors.
________________________________________
Additional Requirements
•	After fixing, test multiple scenarios:
o	Student saves a mood log with valid emotions + completed DASS-21 → must save successfully.
o	Student tries to save without selecting emotions → must block with a validation error.
o	Student tries to save without completing DASS-21 → must block with a validation error.
•	Ensure that the saved logs are viewable later in the student insights and accessible in the admin dashboard.
•	Keep code changes well-commented for future maintainability.

